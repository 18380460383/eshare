<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:padding="8dp"
    android:gravity="top"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:padding="1dp"
        android:id="@+id/adds_ly"
        android:background="@color/publish_press"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <ImageView
            android:layout_gravity="center"
            android:src="@mipmap/publish_add"
            android:scaleType="matrix"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
        <TextView
            android:background="#ffffff"
            android:textSize="20dp"
            android:textColor="@color/publish_text"
            android:layout_gravity="center"
            android:text="点击上传图片"
            android:gravity="center"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
        </LinearLayout>
    <ImageView
        android:layout_centerHorizontal="true"
        android:src="@mipmap/publish_add"
        android:id="@+id/adds_iv"
        android:scaleType="matrix"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
    <TextView
        android:padding="13dp"
        android:text="编辑文字"
        android:background="@drawable/drawable_publish_text"
        android:textSize="20dp"
        android:textColor="@color/publish_text"
        android:id="@+id/adds_tv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
</RelativeLayout>
